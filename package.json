{
  "type": "module",
  "devDependencies": {
    "eslint": "^9.39.2",
    "eslint-plugin-jsdoc": "^50.8.0"
  },
  "scripts": {
    "zip": "rm -f aws-chooser.zip && zip -r aws-chooser.zip popup settings icons manifest.json README.md LICENSE"
  }
}